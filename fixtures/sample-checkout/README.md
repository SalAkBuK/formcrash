# Sample checkout fixtures

Deterministic, fake-only checkout inputs and expected outputs will live here when
Chunk 1 implements the bundled sample. No real customer, address, payment, or
credential data belongs in these fixtures.
